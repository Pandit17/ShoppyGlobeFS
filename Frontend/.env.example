# =REMOVED_SECRET
# ShoppyGlobe React App Environment Variables
# =REMOVED_SECRET

# Base URL of your backend API
VITE_API_URL=REMOVED_SECRET

# Optional: Base URL specifically for products (if separated)
VITE_API_BASE_URL=REMOVED_SECRET

# Optional: PayPal Client ID (if using PayPal integration)
VITE_PAYPAL_CLIENT_ID=REMOVED_SECRET

# Optional: Node environment (development or production)
NODE_ENV=REMOVED_SECRET
